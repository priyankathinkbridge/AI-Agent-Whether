ANTHROPIC_API_KEY=sk-ant-your-key-here
# Optional — powers search_place_info (opening hours, "is X open", etc).
# Without it the agent still works, just without that tool.
BRAVE_API_KEY=your-brave-search-api-key-here
PORT=3000